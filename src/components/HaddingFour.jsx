import React from 'react'

const HaddingFour = ({textFour, className}) => {
  return (
    <>
    <h4 className={`text-xl font-open leading-20 ${className}`}>{textFour}</h4>
    </>
  )
}

export default HaddingFour